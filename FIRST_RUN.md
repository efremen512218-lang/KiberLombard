# 🎬 Первый запуск КиберЛомбард CS2

## Пошаговая инструкция для новичков

### Шаг 1: Установка необходимого ПО

#### Windows:

1. **Python 3.11+**
   - Скачать: https://www.python.org/downloads/
   - При установке отметить "Add Python to PATH"

2. **Node.js 20+**
   - Скачать: https://nodejs.org/
   - Установить LTS версию

3. **Docker Desktop**
   - Скачать: https://www.docker.com/products/docker-desktop/
   - Запустить после установки

4. **Git**
   - Скачать: https://git-scm.com/download/win
   - Установить с настройками по умолчанию

#### macOS:

```bash
# Установить Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить все зависимости
brew install python@3.11 node docker git
```

#### Linux (Ubuntu/Debian):

```bash
# Python
sudo apt update
sudo apt install python3.11 python3-pip

# Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install nodejs

# Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Git
sudo apt install git
```

### Шаг 2: Клонирование проекта

```bash
# Создать папку для проектов
mkdir ~/projects
cd ~/projects

# Клонировать репозиторий (или распаковать архив)
git clone <repository-url> cyber-lombard-cs2
cd cyber-lombard-cs2
```

### Шаг 3: Настройка переменных окружения

```bash
# Скопировать пример
cp .env.example .env

# Открыть в текстовом редакторе
# Windows: notepad .env
# macOS: open -e .env
# Linux: nano .env
```

**Минимальная конфигурация для локального запуска:**

```env
# Steam API (получить на https://steamcommunity.com/dev/apikey)
STEAM_API_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# База данных (оставить как есть для локальной разработки)
DATABASE_URL=postgresql://admin:dev_password_change_in_prod@localhost:5432/cyberlombard

# Redis (оставить как есть)
REDIS_URL=redis://localhost:6379

# JWT Secret (изменить на случайную строку)
JWT_SECRET=your_random_secret_key_min_32_characters_long

# Остальное можно оставить пустым для тестирования
```

### Шаг 4: Запуск базы данных

```bash
# Запустить PostgreSQL и Redis через Docker
docker-compose up -d postgres redis

# Проверить что контейнеры запущены
docker-compose ps

# Должно показать:
# NAME                STATUS
# postgres            Up
# redis               Up
```

### Шаг 5: Установка зависимостей Backend

```bash
cd backend

# Создать виртуальное окружение (рекомендуется)
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Создать таблицы в БД
python -c "from database import engine; from models import Base; Base.metadata.create_all(engine)"

# Вернуться в корень проекта
cd ..
```

### Шаг 6: Установка зависимостей Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Вернуться в корень проекта
cd ..
```

### Шаг 7: Установка зависимостей Steam Bot (опционально)

```bash
cd steam-bot

# Установить зависимости
npm install

# Вернуться в корень проекта
cd ..
```

### Шаг 8: Запуск всех сервисов

Открыть **3 терминала** (или вкладки):

**Терминал 1 - Backend:**
```bash
cd backend
# Активировать venv если не активирован
# Windows: venv\Scripts\activate
# macOS/Linux: source venv/bin/activate

uvicorn main:app --reload --port 8000
```

Должно появиться:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

Должно появиться:
```
▲ Next.js 15.0.0
- Local:        http://localhost:3000
```

**Терминал 3 - Steam Bot (опционально):**
```bash
cd steam-bot
npm run dev
```

### Шаг 9: Открыть приложение

1. Открыть браузер
2. Перейти на http://localhost:3000
3. Увидите главную страницу КиберЛомбард

### Шаг 10: Проверка работоспособности

#### Проверка Backend:

Открыть http://localhost:8000/docs

Должна открыться Swagger документация API.

Попробовать endpoint:
```bash
curl http://localhost:8000/health
```

Ответ:
```json
{
  "status": "ok",
  "timestamp": "2025-12-05T..."
}
```

#### Проверка Frontend:

1. Открыть http://localhost:3000
2. Увидеть главную страницу с калькулятором
3. Попробовать ввести цену скина → увидеть расчет

#### Проверка Steam Bot (если запущен):

```bash
curl http://localhost:3001/health
```

Ответ:
```json
{
  "status": "ok",
  "steam_connected": false,
  "timestamp": "2025-12-05T..."
}
```

`steam_connected: false` - нормально, если не настроены credentials.

### Шаг 11: Тестовый сценарий (без реального Steam)

1. **Главная страница**
   - Открыть http://localhost:3000
   - Ввести цену скина в калькулятор (например, 10000)
   - Увидеть расчет: получишь 6500₽, выкуп 13125₽

2. **"Вход" через Steam** (mock)
   - Нажать "Войти через Steam"
   - Будет редирект на Steam (можно закрыть)
   - Для теста можно вручную перейти на `/cabinet/inventory`

3. **Инвентарь** (mock данные)
   - Backend вернет тестовые скины если Steam API недоступен
   - Выбрать несколько скинов
   - Нажать "Продолжить"

4. **Создание сделки**
   - Увидеть условия сделки
   - Ввести телефон: +79991234567
   - Получить SMS-код (в консоли backend будет выведен код)
   - Ввести код
   - Подписать договор

## Частые проблемы и решения

### ❌ "Connection refused" при запуске Backend

**Причина:** PostgreSQL не запущен

**Решение:**
```bash
docker-compose up -d postgres redis
docker-compose ps  # Проверить статус
```

### ❌ "Module not found" в Python

**Причина:** Зависимости не установлены или venv не активирован

**Решение:**
```bash
cd backend
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
```

### ❌ "Port 3000 already in use"

**Причина:** Порт занят другим приложением

**Решение:**
```bash
# Найти процесс
# Windows:
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# macOS/Linux:
lsof -ti:3000 | xargs kill -9

# Или использовать другой порт
cd frontend
PORT=3001 npm run dev
```

### ❌ "Steam API key invalid"

**Причина:** Неверный или не указан API ключ

**Решение:**
1. Получить ключ на https://steamcommunity.com/dev/apikey
2. Добавить в `.env`:
   ```
   STEAM_API_KEY=ваш_ключ_здесь
   ```
3. Перезапустить backend

### ❌ Docker не запускается

**Причина:** Docker Desktop не запущен

**Решение:**
- Windows/macOS: Запустить Docker Desktop из меню Пуск/Applications
- Linux: `sudo systemctl start docker`

## Следующие шаги

После успешного запуска:

1. **Изучить документацию:**
   - [QUICKSTART.md](QUICKSTART.md) - быстрый старт
   - [ARCHITECTURE.md](ARCHITECTURE.md) - архитектура
   - [STEAM_SETUP.md](STEAM_SETUP.md) - настройка Steam

2. **Настроить реальный Steam:**
   - Получить Steam API ключ
   - Создать бот-аккаунты
   - Настроить 2FA

3. **Интегрировать платежи:**
   - Зарегистрироваться в ЮKassa
   - Получить API ключи
   - Настроить тестовые платежи

4. **Деплой на production:**
   - Следовать [DEPLOYMENT.md](DEPLOYMENT.md)
   - Vercel для Frontend
   - Render/Railway для Backend

## Полезные команды

```bash
# Остановить все Docker контейнеры
docker-compose down

# Очистить БД и начать заново
docker-compose down -v
docker-compose up -d postgres redis

# Посмотреть логи
docker-compose logs postgres
docker-compose logs redis

# Запустить тесты Backend
cd backend
pytest

# Проверить код на ошибки
cd frontend
npm run lint
```

## Получение помощи

- **Документация**: См. файлы `*.md` в корне проекта
- **API Docs**: http://localhost:8000/docs
- **Логи Backend**: В терминале где запущен uvicorn
- **Логи Frontend**: В терминале где запущен npm run dev
- **Логи БД**: `docker-compose logs postgres`

## Контакты

- Email: dev@cyberlombard.ru
- GitHub Issues: [создать issue]
- Telegram: @cyberlombard_support

---

**Поздравляем! 🎉**

Вы успешно запустили КиберЛомбард CS2 локально.

Теперь можно начинать разработку и тестирование.
