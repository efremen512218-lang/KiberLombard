# 🔧 Решение проблем

## Backend не запускается

### Проблема: "ModuleNotFoundError: No module named 'sqlalchemy'"

**Причина**: Не установлены зависимости Python

**Решение**:
```bash
cd backend
pip install -r requirements.txt
```

### Проблема: "ModuleNotFoundError" при запуске с --reload на Windows

**Причина**: Windows multiprocessing не видит установленные пакеты в режиме reload

**Решение**: Запускайте без --reload
```bash
cd backend
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

Или используйте обновленный `start-all.bat` (уже исправлено)

### Проблема: "uvicorn: command not found"

**Причина**: uvicorn не в PATH

**Решение**: Запускайте через Python модуль
```bash
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

### Проблема: "psycopg2-binary" не устанавливается

**Причина**: Требует PostgreSQL и компилятор C

**Решение**: Для разработки используется SQLite, psycopg2 не нужен. Он закомментирован в requirements.txt

## Frontend не подключается к Backend

### Проблема: "Backend не запущен" на странице

**Причина**: Backend не запущен или запущен на другом порту

**Решение**:
1. Проверьте что Backend работает: http://localhost:8000/health
2. Проверьте `frontend/.env.local`:
   ```
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```
3. Перезапустите Frontend (Ctrl+C и `npm run dev`)

### Проблема: CORS ошибки

**Причина**: Frontend на другом домене

**Решение**: Проверьте CORS настройки в `backend/main.py`:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## Steam Bot не подключается

### Проблема: "Invalid credentials"

**Причина**: Неверный логин/пароль или секреты

**Решение**:
1. Проверьте `steam-bot/.env`
2. Убедитесь что Steam Guard включен
3. Проверьте shared_secret и identity_secret

### Проблема: "SteamGuard code required"

**Причина**: Неверный shared_secret

**Решение**: Получите правильный shared_secret через Steam Desktop Authenticator

## База данных

### Проблема: "no such table: users"

**Причина**: БД не создана

**Решение**:
```bash
cd backend
python -c "from database import engine; from models import Base; Base.metadata.create_all(bind=engine)"
```

### Проблема: "database is locked"

**Причина**: SQLite файл используется другим процессом

**Решение**:
1. Остановите все процессы Python
2. Удалите файл `backend/cyberlombard.db`
3. Пересоздайте БД (команда выше)

## Порты заняты

### Проблема: "Address already in use"

**Причина**: Порт уже используется

**Решение**:

**Для Backend (порт 8000)**:
```bash
# Найти процесс
netstat -ano | findstr :8000

# Убить процесс (замените PID)
taskkill /PID <PID> /F
```

**Для Frontend (порт 3000)**:
```bash
# Найти процесс
netstat -ano | findstr :3000

# Убить процесс
taskkill /PID <PID> /F
```

**Для Steam Bot (порт 3001)**:
```bash
# Найти процесс
netstat -ano | findstr :3001

# Убить процесс
taskkill /PID <PID> /F
```

## Python версия

### Проблема: Синтаксические ошибки или "module not found"

**Причина**: Неправильная версия Python

**Решение**: Убедитесь что используется Python 3.10+
```bash
python --version
```

Если версия старая, установите Python 3.10 или новее с python.org

## Node.js версия

### Проблема: Frontend не запускается

**Причина**: Старая версия Node.js

**Решение**: Убедитесь что используется Node.js 18+
```bash
node --version
```

Если версия старая, установите Node.js 18 LTS или новее с nodejs.org

## Общие советы

### Полная переустановка

Если ничего не помогает:

1. **Остановите все процессы**
   ```bash
   # Закройте все окна терминалов
   # Или через Task Manager убейте python.exe и node.exe
   ```

2. **Удалите зависимости**
   ```bash
   # Backend
   cd backend
   pip uninstall -r requirements.txt -y
   
   # Frontend
   cd frontend
   rmdir /s /q node_modules
   del package-lock.json
   
   # Steam Bot
   cd steam-bot
   rmdir /s /q node_modules
   del package-lock.json
   ```

3. **Переустановите**
   ```bash
   setup-dev.bat
   ```

4. **Запустите**
   ```bash
   start-all.bat
   ```

### Проверка работоспособности

**Backend**:
```bash
curl http://localhost:8000/health
```
Должен вернуть: `{"status":"ok","timestamp":"..."}`

**Frontend**:
Откройте http://localhost:3000 в браузере

**Steam Bot**:
```bash
curl http://localhost:3001/health
```
Должен вернуть: `{"status":"ok","steam_connected":true}`

### Логи

Смотрите логи в окнах терминалов, где запущены сервисы. Там будут все ошибки.

### Поддержка

Если проблема не решается:
1. Проверьте все пункты выше
2. Посмотрите логи в терминалах
3. Проверьте версии Python и Node.js
4. Убедитесь что все .env файлы настроены

---

**Большинство проблем решается перезапуском сервисов или переустановкой зависимостей!**
