# ⚡ Инструкция: Перезапуск backend

## Текущая ситуация:
- ✅ Код исправлен
- ✅ Backend работает
- ❌ Но загружает старые тестовые данные (demo_1, demo_2...)
- ❌ Нужен перезапуск чтобы применить изменения

---

## Как перезапустить:

### Вариант 1: Через терминал где запущен backend

#### Шаг 1: Найди окно терминала где запущен backend
Там должно быть что-то вроде:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

#### Шаг 2: Останови backend
```
Нажми Ctrl+C в этом терминале
```

#### Шаг 3: Запусти заново
```bash
python main.py
```

---

### Вариант 2: Через новый терминал

#### Шаг 1: Открой новый терминал

#### Шаг 2: Найди процесс backend
```bash
netstat -ano | findstr :8000
```

Увидишь что-то вроде:
```
TCP    0.0.0.0:8000    0.0.0.0:0    LISTENING    12345
```

Последнее число (12345) - это PID процесса

#### Шаг 3: Останови процесс
```bash
taskkill /PID 12345 /F
```
(Замени 12345 на твой PID)

#### Шаг 4: Запусти backend заново
```bash
cd backend
python main.py
```

---

### Вариант 3: Через start-all.bat

Если ты запускал через `start-all.bat`:

#### Шаг 1: Закрой все окна терминалов
Закрой окна где запущены backend и frontend

#### Шаг 2: Запусти заново
```bash
start-all.bat
```

---

## Проверка что перезапуск прошел успешно:

### Тест 1: Проверь логи backend
При запуске должно быть:
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000
```

### Тест 2: Проверь API
```bash
curl http://localhost:8000/api/inventory/76561198306528518
```

Должны быть **реальные** assetid (не demo_1, demo_2...)

### Тест 3: Загрузи инвентарь в браузере
```
http://localhost:3000/cabinet/inventory
→ 📥 Загрузить инвентарь
→ Вставь ссылку
→ Загрузить
```

В логах backend должно быть:
```
[PUBLIC_INVENTORY] Status: 200 for 76561198306528518
[PUBLIC_INVENTORY] Найдено 81 предметов
[PUBLIC_INVENTORY] ✅ Загружено X tradable предметов
```

---

## Если не получается:

### Проблема: Не могу найти терминал с backend
**Решение:** Используй Вариант 2 (найди процесс через netstat)

### Проблема: Процесс не останавливается
**Решение:** 
```bash
# Найди PID
netstat -ano | findstr :8000

# Останови принудительно
taskkill /PID <PID> /F
```

### Проблема: После перезапуска все равно demo данные
**Решение:** 
1. Убедись что файлы сохранены (Ctrl+S)
2. Проверь что запускаешь из правильной папки (backend/)
3. Проверь что нет других процессов на порту 8000

---

## Быстрая команда (все в одном):

```bash
# Останови процесс на порту 8000
for /f "tokens=5" %a in ('netstat -ano ^| findstr :8000') do taskkill /PID %a /F

# Запусти backend
cd backend
python main.py
```

---

## После перезапуска:

1. ✅ Обнови браузер (F5)
2. ✅ Загрузи инвентарь снова
3. ✅ Проверь что загружаются реальные предметы (81 штука)
4. ✅ Проверь что нет ошибок 404 для изображений

---

**Перезапусти backend и все заработает! 🚀**
