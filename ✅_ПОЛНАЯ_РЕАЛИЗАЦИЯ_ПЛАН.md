# ✅ Полная Production Реализация - Детальный План

## Текущий Статус

### ✅ Готово (40%)
1. **Модели БД** - все 4 новые модели добавлены
2. **Экономическая модель** - коэффициенты 50-65%, расчёты прибыли
3. **Pricing Service** - полная логика расчётов
4. **API документация** - market.csgo изучен
5. **Базовые сервисы** - SMS, KYC, Contract, SBP (с fake провайдерами)

### 🔄 В Процессе (30%)
6. **Market.CSGO интеграция** - создан real_market_csgo.py
7. **Price Aggregator** - логика готова, нужна интеграция

### ⏳ Осталось (30%)
8. **API Endpoints** - KYC, signing, payouts
9. **Frontend компоненты** - слайдер, формы, подписание
10. **Тестирование** - unit, integration, E2E

---

## Детальный План Реализации

### ФАЗА 1: Backend API (6-8 часов)

#### 1.1 Миграция БД (30 мин)
```bash
cd backend

# Создать миграцию
alembic revision --autogenerate -m "add_production_models"

# Применить
alembic upgrade head

# Проверить
python -c "from models import *; print('✅ Модели загружены')"
```

**Результат:**
- ✅ kyc_verifications
- ✅ deal_signatures
- ✅ sbp_payouts
- ✅ audit_logs (обновлена)

#### 1.2 Обновить Price Aggregator (1 час)
```python
# backend/services/price_aggregator.py

from services.real_market_csgo import RealMarketCSGOService
from services.lisskins_service import LisSkinsService
from services.steam_prices import SteamPriceService

async def get_prices(items):
    # Параллельно
    steam_task = SteamPriceService.get_prices(items)
    market_task = RealMarketCSGOService.get_prices(items)
    lis_task = LisSkinsService.get_prices(items)  # Пока демо
    
    steam, market, lis = await asyncio.gather(...)
    
    # Агрегация
    for item in items:
        instant_price = min(market[item], lis[item]) if both > 0
        is_acceptable = instant_price >= 20 and lis[item] > 0
```

#### 1.3 KYC Endpoints (2 часа)
```python
# backend/main.py

@app.post("/api/kyc/phone/send-code")
async def send_phone_code(phone: str, db: Session = Depends(get_db)):
    code = SMSService.create_verification(db, phone, "phone_verify")
    return {"success": True, "dev_code": code}

@app.post("/api/kyc/phone/verify")
async def verify_phone(phone: str, code: str, user_id: int, db: Session = Depends(get_db)):
    if not SMSService.verify_code(db, phone, code, "phone_verify"):
        raise HTTPException(400, "Неверный код")
    
    user = db.query(User).get(user_id)
    user.phone = phone
    user.phone_verified = True
    db.commit()
    
    return {"success": True}

@app.post("/api/kyc/passport/submit")
async def submit_passport(
    user_id: int,
    passport_data: PassportData,
    db: Session = Depends(get_db)
):
    verification = KYCVerification(
        user_id=user_id,
        full_name=passport_data.full_name,
        birth_date=passport_data.birth_date,
        passport_series=passport_data.series,
        passport_number=passport_data.number,
        issued_by=passport_data.issued_by,
        issue_date=passport_data.issue_date,
        department_code=passport_data.department_code,
        photo_url=passport_data.photo_url,
        status="pending"
    )
    db.add(verification)
    db.commit()
    
    # Отправить в KYC провайдер
    result = await KYCService.submit_verification(verification.id, passport_data)
    
    return {"verification_id": verification.id, "status": "pending"}

@app.get("/api/kyc/status/{verification_id}")
async def get_kyc_status(verification_id: int, db: Session = Depends(get_db)):
    verification = db.query(KYCVerification).get(verification_id)
    return {"status": verification.status}
```

#### 1.4 Contract Signing Endpoints (2 часа)
```python
@app.post("/api/deals/{deal_id}/sign/init")
async def init_signing(
    deal_id: int,
    request: Request,
    db: Session = Depends(get_db)
):
    deal = db.query(Deal).get(deal_id)
    user = deal.user
    
    # Генерировать договор
    contract_html = ContractService.generate_contract_html(deal, user)
    contract_hash = ContractService.calculate_hash(contract_html)
    
    # Отправить SMS код
    code = SMSService.create_verification(db, user.phone, "contract_sign")
    
    return {
        "contract_html": contract_html,
        "contract_hash": contract_hash,
        "dev_code": code
    }

@app.post("/api/deals/{deal_id}/sign/complete")
async def complete_signing(
    deal_id: int,
    code: str,
    contract_hash: str,
    request: Request,
    db: Session = Depends(get_db)
):
    deal = db.query(Deal).get(deal_id)
    user = deal.user
    
    # Проверить код
    if not SMSService.verify_code(db, user.phone, code, "contract_sign"):
        raise HTTPException(400, "Неверный код")
    
    # Создать подпись
    signature = DealSignature(
        deal_id=deal_id,
        user_id=user.id,
        phone=user.phone,
        signature_code_hash=SMSService.hash_code(code),
        signature_timestamp=datetime.now(),
        ip_address=request.client.host,
        user_agent=request.headers.get("user-agent"),
        contract_hash=contract_hash,
        contract_version="v1.0",
        terms_accepted_at=datetime.now()
    )
    db.add(signature)
    
    # Обновить сделку
    deal.deal_status = DealStatus.SIGNED
    db.commit()
    
    # Генерировать PDF
    pdf_path = ContractService.generate_pdf(deal, user, signature)
    signature.contract_pdf_url = pdf_path
    db.commit()
    
    return {"success": True, "pdf_url": pdf_path}
```

#### 1.5 Payout Endpoints (1 час)
```python
@app.post("/api/deals/{deal_id}/payout/create")
async def create_payout(deal_id: int, db: Session = Depends(get_db)):
    deal = db.query(Deal).get(deal_id)
    user = deal.user
    
    # Создать выплату
    payout_id = await SBPService.create_payout(
        phone=user.phone,
        amount=deal.loan_amount,
        deal_id=deal_id
    )
    
    payout = SBPPayout(
        deal_id=deal_id,
        payout_id=payout_id,
        phone=user.phone,
        amount=deal.loan_amount,
        status="pending"
    )
    db.add(payout)
    db.commit()
    
    return {"payout_id": payout_id, "status": "pending"}

@app.get("/api/deals/{deal_id}/payout/status")
async def get_payout_status(deal_id: int, db: Session = Depends(get_db)):
    payout = db.query(SBPPayout).filter_by(deal_id=deal_id).first()
    
    # Обновить статус
    status = await SBPService.check_status(payout.payout_id)
    payout.status = status
    db.commit()
    
    return {"status": status}
```

#### 1.6 Middleware для Логирования (30 мин)
```python
# backend/middleware.py

@app.middleware("http")
async def audit_middleware(request: Request, call_next):
    # Захватить IP и user-agent
    request.state.ip_address = request.client.host
    request.state.user_agent = request.headers.get("user-agent")
    
    response = await call_next(request)
    return response

# Использование в endpoints
def log_action(db, user_id, deal_id, action, details):
    log = AuditLog(
        user_id=user_id,
        deal_id=deal_id,
        action=action,
        details=details,
        ip_address=request.state.ip_address,
        user_agent=request.state.user_agent,
        session_id=request.cookies.get("session_id")
    )
    db.add(log)
    db.commit()
```

---

### ФАЗА 2: Frontend (6-8 часов)

#### 2.1 TermSlider Component (1 час)
```typescript
// frontend/src/components/TermSlider.tsx

export function TermSlider({ value, onChange, loanAmount }) {
  const [days, setDays] = useState(value || 14);
  const [buybackPrice, setBuybackPrice] = useState(0);
  
  useEffect(() => {
    // Рассчитать выкуп
    const config = getTermConfig(days);
    const base = loanAmount * (1 + config.interest);
    const buyback = base * (1 + config.premium);
    setBuybackPrice(buyback);
  }, [days, loanAmount]);
  
  return (
    <div className="term-slider">
      <label>Срок выкупа: {days} дней</label>
      <input
        type="range"
        min={7}
        max={30}
        value={days}
        onChange={(e) => {
          const newDays = Number(e.target.value);
          setDays(newDays);
          onChange(newDays);
        }}
      />
      <div className="term-info">
        <div>Выкуп до: {calculateExpiryDate(days)}</div>
        <div>Сумма выкупа: {buybackPrice.toFixed(2)} ₽</div>
      </div>
    </div>
  );
}
```

#### 2.2 PhoneVerification Component (1 час)
```typescript
// frontend/src/components/PhoneVerification.tsx

export function PhoneVerification({ userId, onVerified }) {
  const [phone, setPhone] = useState('');
  const [code, setCode] = useState('');
  const [codeSent, setCodeSent] = useState(false);
  
  const sendCode = async () => {
    const res = await fetch('/api/kyc/phone/send-code', {
      method: 'POST',
      body: JSON.stringify({ phone })
    });
    setCodeSent(true);
  };
  
  const verifyCode = async () => {
    const res = await fetch('/api/kyc/phone/verify', {
      method: 'POST',
      body: JSON.stringify({ phone, code, user_id: userId })
    });
    if (res.ok) {
      onVerified();
    }
  };
  
  return (
    <div className="phone-verification">
      <input
        type="tel"
        placeholder="+7XXXXXXXXXX"
        value={phone}
        onChange={(e) => setPhone(e.target.value)}
      />
      {!codeSent ? (
        <button onClick={sendCode}>Отправить код</button>
      ) : (
        <>
          <input
            type="text"
            placeholder="Код из SMS"
            value={code}
            onChange={(e) => setCode(e.target.value)}
          />
          <button onClick={verifyCode}>Подтвердить</button>
        </>
      )}
    </div>
  );
}
```

#### 2.3 PassportForm Component (2 часа)
```typescript
// frontend/src/components/PassportForm.tsx

export function PassportForm({ userId, onSubmitted }) {
  const [formData, setFormData] = useState({
    full_name: '',
    birth_date: '',
    series: '',
    number: '',
    issued_by: '',
    issue_date: '',
    department_code: '',
    photo: null
  });
  
  const handleSubmit = async () => {
    // Upload photo first
    const photoUrl = await uploadPhoto(formData.photo);
    
    // Submit passport data
    const res = await fetch('/api/kyc/passport/submit', {
      method: 'POST',
      body: JSON.stringify({
        user_id: userId,
        ...formData,
        photo_url: photoUrl
      })
    });
    
    if (res.ok) {
      const { verification_id } = await res.json();
      onSubmitted(verification_id);
    }
  };
  
  return (
    <form className="passport-form">
      <input placeholder="Фамилия Имя Отчество" ... />
      <input type="date" placeholder="Дата рождения" ... />
      <input placeholder="Серия" ... />
      <input placeholder="Номер" ... />
      <textarea placeholder="Кем выдан" ... />
      <input type="date" placeholder="Дата выдачи" ... />
      <input placeholder="Код подразделения" ... />
      <input type="file" accept="image/*" ... />
      <button onClick={handleSubmit}>Отправить</button>
    </form>
  );
}
```

#### 2.4 ContractSigning Component (2 часа)
```typescript
// frontend/src/components/ContractSigning.tsx

export function ContractSigning({ dealId, onSigned }) {
  const [contractHtml, setContractHtml] = useState('');
  const [contractHash, setContractHash] = useState('');
  const [agreed, setAgreed] = useState(false);
  const [code, setCode] = useState('');
  const [codeSent, setCodeSent] = useState(false);
  
  useEffect(() => {
    // Load contract
    fetch(`/api/deals/${dealId}/sign/init`, { method: 'POST' })
      .then(res => res.json())
      .then(data => {
        setContractHtml(data.contract_html);
        setContractHash(data.contract_hash);
      });
  }, [dealId]);
  
  const sendSignatureCode = async () => {
    // Code already sent in init
    setCodeSent(true);
  };
  
  const completeSignature = async () => {
    const res = await fetch(`/api/deals/${dealId}/sign/complete`, {
      method: 'POST',
      body: JSON.stringify({ code, contract_hash: contractHash })
    });
    
    if (res.ok) {
      onSigned();
    }
  };
  
  return (
    <div className="contract-signing">
      <iframe srcDoc={contractHtml} />
      
      <label>
        <input
          type="checkbox"
          checked={agreed}
          onChange={(e) => setAgreed(e.target.checked)}
        />
        Я согласен с условиями договора
      </label>
      
      {!codeSent ? (
        <button disabled={!agreed} onClick={sendSignatureCode}>
          Подписать (отправить SMS)
        </button>
      ) : (
        <>
          <input
            placeholder="Код из SMS"
            value={code}
            onChange={(e) => setCode(e.target.value)}
          />
          <button onClick={completeSignature}>
            Подтвердить подпись
          </button>
        </>
      )}
    </div>
  );
}
```

#### 2.5 Обновить ItemCard (1 час)
```typescript
// frontend/src/components/ItemCard.tsx

export function ItemCard({ item }) {
  return (
    <div className="item-card">
      <img src={item.icon_url} />
      <h3>{item.name}</h3>
      
      <div className="prices">
        <div>💎 Steam: {item.steam_price} ₽</div>
        <div>⚡ Lis-Skins: {item.lis_price} ₽</div>
        <div>📊 market.csgo: {item.market_csgo_price} ₽</div>
      </div>
      
      {item.is_acceptable ? (
        <div className="loan-amount">
          💰 Выдадим: {item.loan_price} ₽
          <small>Коэффициент: {item.loan_percent * 100}%</small>
        </div>
      ) : (
        <div className="not-acceptable">
          ❌ Не принимаем
          <small>Нет моментальной продажи</small>
        </div>
      )}
    </div>
  );
}
```

---

### ФАЗА 3: Тестирование (4-6 часов)

#### 3.1 Unit Тесты (2 часа)
```python
# backend/tests/test_pricing.py

def test_purchase_coefficient():
    assert get_purchase_coefficient(200) == 0.50
    assert get_purchase_coefficient(2000) == 0.55
    assert get_purchase_coefficient(20000) == 0.60
    assert get_purchase_coefficient(200000) == 0.65

def test_profit_default():
    profit = calculate_profit_default(2800, 1540)
    assert profit["profit"] > 1000
    assert profit["margin_percent"] > 70

def test_profit_buyback():
    profit = calculate_profit_buyback(1540, 14)
    assert profit["profit"] > 400
    assert profit["annual_rate_percent"] > 700
```

#### 3.2 Integration Тесты (2 часа)
```python
# backend/tests/test_deal_flow.py

async def test_full_deal_flow():
    # 1. Create user
    user = create_test_user()
    
    # 2. Verify phone
    code = send_phone_code(user.phone)
    verify_phone(user.phone, code)
    
    # 3. Submit KYC
    verification_id = submit_passport(user.id, test_passport_data)
    wait_for_kyc_approval(verification_id)
    
    # 4. Create deal
    deal = create_deal(user.id, test_items, 14)
    
    # 5. Sign contract
    contract_code = init_signing(deal.id)
    complete_signing(deal.id, contract_code)
    
    # 6. Create payout
    payout_id = create_payout(deal.id)
    wait_for_payout_completion(payout_id)
    
    # Verify
    assert deal.status == "ACTIVE"
    assert payout.status == "completed"
```

#### 3.3 E2E Тесты (2 часа)
```typescript
// frontend/tests/e2e/deal-flow.spec.ts

test('full deal flow', async ({ page }) => {
  // 1. Login
  await page.goto('/auth/steam');
  await loginWithSteam();
  
  // 2. Go to inventory
  await page.goto('/cabinet/inventory');
  await page.waitForSelector('.item-card');
  
  // 3. Select items
  await page.click('.item-card:nth-child(1)');
  await page.click('.item-card:nth-child(2)');
  
  // 4. Create quote
  await page.click('button:has-text("Создать сделку")');
  
  // 5. Select term
  await page.fill('input[type="range"]', '14');
  
  // 6. Verify phone
  await page.fill('input[placeholder="+7XXXXXXXXXX"]', '+79001234567');
  await page.click('button:has-text("Отправить код")');
  await page.fill('input[placeholder="Код из SMS"]', '123456');
  await page.click('button:has-text("Подтвердить")');
  
  // 7. Submit passport
  await fillPassportForm();
  await page.click('button:has-text("Отправить")');
  
  // 8. Sign contract
  await page.check('input[type="checkbox"]');
  await page.click('button:has-text("Подписать")');
  await page.fill('input[placeholder="Код из SMS"]', '654321');
  await page.click('button:has-text("Подтвердить подпись")');
  
  // 9. Verify success
  await expect(page.locator('text=Сделка создана')).toBeVisible();
});
```

---

## Итоговый Чеклист

### Backend
- [x] Модели БД
- [ ] Миграция БД
- [ ] Real API интеграции
- [ ] KYC endpoints
- [ ] Signing endpoints
- [ ] Payout endpoints
- [ ] Middleware
- [ ] Unit тесты
- [ ] Integration тесты

### Frontend
- [ ] TermSlider
- [ ] PhoneVerification
- [ ] PassportForm
- [ ] ContractSigning
- [ ] ItemCard обновлён
- [ ] E2E тесты

### DevOps
- [ ] .env настроен
- [ ] API ключи получены
- [ ] Deployment guide
- [ ] Monitoring setup

---

## Команды для Запуска

```bash
# 1. Миграция
cd backend
python alembic_migration_add_models.py

# 2. Тесты
pytest tests/

# 3. Запуск backend
python main.py

# 4. Запуск frontend
cd ../frontend
npm run dev

# 5. E2E тесты
npm run test:e2e
```

---

## ETA: 16-22 часа

- Backend: 6-8 часов
- Frontend: 6-8 часов
- Тестирование: 4-6 часов

**Готово к production через 3-4 рабочих дня**
