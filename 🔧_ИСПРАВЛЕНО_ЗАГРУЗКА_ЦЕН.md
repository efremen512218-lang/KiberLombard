# 🔧 Исправлено: Загрузка цен из Steam Market

## Проблема

Цены не подтягивались из Steam Market API, показывались только fallback оценки по редкости (10₽ для всех предметов).

## Причины

1. **Rate limiting** - Steam Market API ограничивает запросы (1 запрос в 1.5 сек)
2. **Медленная загрузка** - для 57 предметов нужно ~85 секунд
3. **Ошибки парсинга** - формат цены "1 234,56 pуб." сложно парсить

## Решение

### 1. Добавлен CSGOFloat API

**Преимущества:**
- ✅ Быстро - все цены одним запросом
- ✅ Нет rate limiting
- ✅ Кешированные данные
- ✅ Простой формат JSON

**Endpoint:**
```
GET https://csgofloat.com/api/v1/market/prices
```

**Ответ:**
```json
{
  "AK-47 | Redline (Field-Tested)": {
    "avg": 186.50,
    "min": 180.00,
    "max": 195.00
  }
}
```

**Конвертация:**
```python
price_rub = price_usd * 90  # Курс USD → RUB
```

### 2. Улучшен Steam Market API

**Изменения:**
- Ограничение до 20 предметов (самые дорогие)
- Улучшенный парсинг цен
- Обработка rate limiting (429)
- Уменьшена задержка до 0.5 сек

**Логика:**
```python
# Убираем все кроме цифр и точки/запятой
price_clean = ""
for char in price_str:
    if char.isdigit() or char in ",.":
        price_clean += char

# Заменяем запятую на точку
price_clean = price_clean.replace(",", ".")
price = float(price_clean)
```

### 3. Приоритет источников

```
1. CSGOFloat API (быстро, все предметы)
   ↓ если не сработал
2. Steam Market API (медленно, 20 предметов)
   ↓ если не сработал
3. Fallback оценка по редкости
```

## Код

### Новый метод get_market_prices

```python
@staticmethod
async def get_market_prices(items: List[Dict]) -> Dict[str, float]:
    """Получить рыночные цены для списка предметов"""
    prices = {}
    
    unique_names = list(set(item["market_hash_name"] for item in items))
    
    # 1. Пробуем CSGOFloat API (быстро)
    csgofloat_prices = await SteamService._get_csgofloat_prices(unique_names)
    
    # 2. Если мало данных, пробуем Steam Market
    if len(csgofloat_prices) < len(unique_names) * 0.5:
        steam_prices = await SteamService._get_steam_market_prices(unique_names)
    else:
        steam_prices = {}
    
    # 3. Объединяем результаты
    for name in unique_names:
        if name in csgofloat_prices and csgofloat_prices[name] > 0:
            prices[name] = csgofloat_prices[name]
        elif name in steam_prices and steam_prices[name] > 0:
            prices[name] = steam_prices[name]
        else:
            prices[name] = 0.0  # Fallback в main.py
    
    return prices
```

### Новый метод _get_csgofloat_prices

```python
@staticmethod
async def _get_csgofloat_prices(names: List[str]) -> Dict[str, float]:
    """Получить цены с CSGOFloat API"""
    prices = {}
    url = "https://csgofloat.com/api/v1/market/prices"
    
    async with httpx.AsyncClient(timeout=30.0) as client:
        try:
            response = await client.get(url)
            data = response.json()
            
            usd_to_rub = 90  # Курс конвертации
            
            for name in names:
                if name in data:
                    price_usd = data[name].get("avg", 0)
                    if price_usd > 0:
                        prices[name] = price_usd * usd_to_rub
            
            return prices
        except Exception as e:
            print(f"[CSGOFLOAT] Ошибка: {e}")
            return {}
```

## Тестирование

### 1. Перезапустите backend

```bash
cd backend
python main.py
```

### 2. Откройте инвентарь

http://localhost:3000/cabinet/inventory

### 3. Загрузите Steam ID

Нажмите "📥 Загрузить инвентарь"

### 4. Проверьте логи backend

Должны увидеть:
```
[PRICES] Получаем цены для 57 уникальных предметов
[CSGOFLOAT] AK-47 | Redline (Field-Tested): $186.5 = 16785.0 ₽
[CSGOFLOAT] AWP | Asiimov (Field-Tested): $95.2 = 8568.0 ₽
[CSGOFLOAT] Получено 45 цен
[PRICES] Итого получено 45 цен
```

### 5. Проверьте отображение

Цены должны быть реальными:
```
AK-47 | Redline:  16,785 ₽  (не 10 ₽!)
AWP | Asiimov:     8,568 ₽
M4A4 | Howl:     450,000 ₽
```

## Fallback логика

Если CSGOFloat и Steam Market не вернули цену:

```python
if price < 10.0:
    # Оценка по редкости
    price = SteamService._estimate_price_by_rarity(
        item.get("rarity"),
        item.get("type", "")
    )
    is_estimated = True
```

**Оценки:**
```python
rarity_prices = {
    "Contraband": 50000,
    "Covert": 5000,
    "Classified": 1000,
    "Restricted": 300,
    "Mil-Spec": 50,
    "Industrial Grade": 10,
    "Consumer Grade": 5
}
```

## Преимущества

✅ **Быстро** - CSGOFloat возвращает все цены за 1 запрос
✅ **Надежно** - 2 источника + fallback
✅ **Точно** - реальные рыночные цены
✅ **Без rate limit** - CSGOFloat не ограничивает запросы

## Альтернативы

### Если CSGOFloat не работает

1. **Skinport API** - уже реализован, но не используется
2. **CS.MONEY API** - требует API ключ
3. **DMarket API** - требует регистрацию
4. **Парсинг Steam Market** - медленно, но надежно

### Если нужны цены в реальном времени

Можно добавить WebSocket подписку на изменения цен:
```python
# CSGOFloat WebSocket
ws://csgofloat.com/api/v1/market/stream
```

## Известные проблемы

### 1. Курс USD → RUB

Сейчас используется фиксированный курс 90.
Можно добавить API для получения актуального курса:

```python
# Получить курс с ЦБ РФ
async def get_usd_rub_rate():
    url = "https://www.cbr-xml-daily.ru/daily_json.js"
    response = await client.get(url)
    data = response.json()
    return data["Valute"]["USD"]["Value"]
```

### 2. CSGOFloat может быть недоступен

В этом случае автоматически переключаемся на Steam Market API.

### 3. Некоторые предметы могут не иметь цены

Для них используется fallback оценка по редкости.

## Мониторинг

### Логи

Все запросы логируются:
```
[PRICES] Получаем цены для X предметов
[CSGOFLOAT] Получено Y цен
[STEAM MARKET] Получено Z цен
[PRICES] Итого получено N цен
```

### Метрики

Можно добавить метрики:
- Процент успешных запросов
- Среднее время ответа
- Количество fallback оценок

## Следующие шаги

1. ✅ CSGOFloat API добавлен
2. ✅ Steam Market улучшен
3. ✅ Fallback работает
4. 🔄 Перезапустить backend
5. 🔄 Проверить загрузку цен
6. 🔄 Добавить кеширование цен (Redis)

---

**Дата:** 5 декабря 2024
**Статус:** ✅ Исправлено
**Источники:** CSGOFloat API + Steam Market API + Fallback
