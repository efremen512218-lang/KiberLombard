# 🏗 Архитектура КиберЛомбард CS2

## Общая схема

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│   Browser   │─────▶│   Next.js    │─────▶│   FastAPI   │
│  (Client)   │◀─────│  (Frontend)  │◀─────│  (Backend)  │
└─────────────┘      └──────────────┘      └─────────────┘
                                                   │
                                                   ▼
                     ┌──────────────┐      ┌─────────────┐
                     │  Steam Bot   │◀────▶│ PostgreSQL  │
                     │   (Node.js)  │      │   + Redis   │
                     └──────────────┘      └─────────────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  Steam API   │
                     └──────────────┘
```

## Компоненты

### Frontend (Next.js 15 + TypeScript)

**Технологии:**
- Next.js 15 (App Router)
- TypeScript
- Tailwind CSS
- Axios для API запросов

**Страницы:**
- `/` - Landing page с калькулятором
- `/cabinet/inventory` - Инвентарь CS2 с выбором скинов
- `/cabinet/quote` - Создание сделки (3 шага: условия → KYC → подпись)
- `/cabinet/deals` - История сделок
- `/cabinet/deals/[id]` - Детали сделки + выкуп

**Особенности:**
- Server-side rendering для SEO
- Кибер-дизайн (темная тема + неон)
- Мобильная адаптация
- Real-time обновления (будущее: WebSocket)

### Backend (FastAPI + Python)

**Технологии:**
- FastAPI (async)
- SQLAlchemy (ORM)
- PostgreSQL (БД)
- Redis (кеш + сессии)
- Alembic (миграции)

**Основные модули:**

1. **Auth** (`/api/auth/*`)
   - Steam OpenID верификация
   - JWT токены

2. **KYC** (`/api/kyc/*`)
   - SMS-верификация телефона
   - Паспортные данные (115-ФЗ)

3. **Inventory** (`/api/inventory/*`)
   - Получение CS2 инвентаря через Steam API
   - Кеширование в Redis (TTL 1 час)

4. **Pricing** (`/api/quote`)
   - Агрегация цен с маркетов (Skinport, CSFloat, Steam Market)
   - Расчет условий сделки

5. **Deals** (`/api/deals/*`)
   - CRUD операции со сделками
   - Генерация PDF договоров
   - Управление статусами

**Сервисы:**

- `SteamService` - работа с Steam API
- `PricingService` - расчет цен и условий
- `SMSService` - отправка и верификация SMS
- `ContractService` - генерация PDF договоров

### Steam Bot (Node.js)

**Технологии:**
- steam-user (авторизация)
- steam-tradeoffer-manager (трейды)
- steam-totp (2FA)
- Express (API)

**Функции:**
- Автоматическое принятие трейдов
- Создание trade offers
- Подтверждение через мобильный аутентификатор
- Уведомление backend о статусах

**API Endpoints:**
- `POST /api/trade/create` - создать трейд (получение скинов)
- `POST /api/trade/reverse` - обратный трейд (возврат скинов)
- `GET /api/inventory` - инвентарь бота
- `GET /health` - health check

### База данных (PostgreSQL)

**Таблицы:**

1. **users**
   - Steam ID, username, avatar
   - Телефон + верификация
   - Паспортные данные
   - Метаданные (created_at, last_login, is_blocked)

2. **deals**
   - Финансовые условия (market_total, loan_amount, buyback_price)
   - Сроки (created_at, option_expiry, buyback_at)
   - Статус (PENDING, ACTIVE, BUYBACK, DEFAULT, CANCELLED)
   - Snapshot предметов (JSONB)
   - Договор (PDF URL, ПЭП)

3. **steam_trades**
   - Trade offer ID и URL
   - Направление (incoming/outgoing)
   - Статус
   - Связь с deal

4. **sms_verifications**
   - Телефон, хеш кода
   - Назначение (phone_verify, deal_sign, buyback)
   - Timestamps

5. **audit_logs**
   - Все действия пользователей
   - IP, user agent
   - Детали в JSON

## Бизнес-логика

### Создание сделки (Deal Flow)

```
1. Клиент выбирает скины в инвентаре
   ↓
2. Backend рассчитывает quote:
   - Получает цены с маркетов
   - loan_amount = market_total * 0.65
   - buyback_price = market_total * 1.15 + loan_amount * 0.25
   ↓
3. KYC проверка:
   - Телефон (SMS) - всегда
   - Паспорт - если сумма > 15к₽
   ↓
4. Подписание договора:
   - Клиент читает условия
   - Ставит чекбокс
   - Вводит SMS-код (ПЭП)
   ↓
5. Создание сделки в БД:
   - Статус: PENDING
   - Генерация PDF договора
   - Snapshot предметов
   ↓
6. Steam Bot создает trade offer:
   - Отправляет клиенту
   - Клиент принимает в Steam
   ↓
7. Bot подтверждает получение:
   - Статус → ACTIVE
   - Backend инициирует выплату (ЮKassa/СБП)
   ↓
8. Клиент может:
   a) Выкупить до срока:
      - Оплачивает buyback_price
      - Получает обратный трейд
      - Статус → BUYBACK
   
   b) Не выкупить:
      - Срок истекает
      - Статус → DEFAULT
      - Скины остаются сервису
```

### Pricing Logic

```python
def calculate_quote(items, option_days=14):
    # 1. Получить цены с маркетов
    market_total = sum(get_market_price(item) for item in items)
    
    # 2. Сумма выплаты (65%)
    loan_amount = market_total * 0.65
    
    # 3. Цена выкупа (115% + премия 25%)
    base_buyback = market_total * 1.15
    option_premium = loan_amount * 0.25
    buyback_price = base_buyback + option_premium
    
    # 4. Срок опциона
    option_expiry = now() + timedelta(days=option_days)
    
    return {
        'market_total': market_total,
        'loan_amount': loan_amount,
        'buyback_price': buyback_price,
        'option_expiry': option_expiry
    }
```

### Антифрод

**Лимиты:**
- Без паспорта: до 15к₽
- С паспортом: до 600к₽
- Сделок в день: 3 (новый аккаунт), 10 (верифицированный)

**Блокировки:**
- Новый Steam аккаунт (<30 дней) + дорогие скины (>50к)
- Множественные аккаунты с одного IP
- Подозрительные паттерны (частые сделки без выкупа)

**Ручная модерация:**
- Сделки >100к₽
- Фото паспорта
- Первая сделка нового пользователя

## Безопасность

### Данные

- Паспорта: шифрование в БД (AES-256)
- SMS-коды: только хеши (SHA-256)
- JWT токены: короткий TTL (7 дней)
- API ключи: в переменных окружения

### Сеть

- HTTPS обязательно
- CORS: только свои домены
- Rate limiting: 100 req/min
- DDoS защита: Cloudflare

### Юридическая

- Все дисклеймеры крупным шрифтом
- Обязательные чекбоксы
- ПЭП через SMS
- Хранение всех доказательств 5 лет

## Масштабирование

### Горизонтальное

- Backend: несколько инстансов за load balancer (nginx/AWS ALB)
- Steam Bot: несколько ботов (round-robin через Redis queue)
- Frontend: CDN (Vercel Edge Network)

### Вертикальное

- PostgreSQL: увеличение ресурсов при росте
- Redis: Redis Cluster для кеша
- Индексы БД для частых запросов

### Оптимизации

- Кеширование инвентарей (Redis, TTL 1 час)
- Кеширование цен (Redis, TTL 10 минут)
- Lazy loading больших инвентарей
- CDN для статики и изображений
- Database connection pooling

## Мониторинг

### Метрики

- Количество сделок (по статусам)
- Средняя сумма сделки
- Conversion rate (инвентарь → сделка)
- Процент выкупов vs дефолтов
- Время обработки трейдов

### Алерты

- Steam Bot отключен
- Ошибки трейдов
- Истечение сроков опционов (cron)
- Подозрительная активность
- Ошибки платежей

### Логирование

- Структурированные логи (JSON)
- Уровни: DEBUG, INFO, WARNING, ERROR
- Централизованное хранение (ELK/Loki)
- Retention: 30 дней

## Деплой

### Production Stack

- **Frontend**: Vercel (Next.js)
- **Backend**: Render/Railway (FastAPI)
- **Steam Bot**: DigitalOcean Droplet (PM2)
- **Database**: Supabase/Neon (PostgreSQL)
- **Cache**: Upstash (Redis)
- **CDN**: Cloudflare
- **Monitoring**: Sentry + Grafana

### CI/CD

```yaml
# GitHub Actions
on: [push]
jobs:
  test:
    - pytest backend/tests
    - npm test frontend
  
  deploy:
    - vercel deploy (frontend)
    - render deploy (backend)
    - ssh deploy (steam-bot)
```

## Будущие улучшения

1. **WebSocket** для real-time обновлений статусов
2. **Маркетплейс** для продажи дефолтных скинов
3. **Мобильное приложение** (React Native)
4. **Автовыкуп** при достижении целевой цены
5. **Реферальная программа**
6. **Поддержка других игр** (Dota 2, TF2, Rust)
