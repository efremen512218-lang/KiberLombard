# 🎮 Методы загрузки Steam инвентаря

## Обзор

Система поддерживает несколько методов загрузки инвентаря Steam CS2:

### 1. **Публичный метод (основной)** ✅
- Работает для открытых инвентарей
- Не требует авторизации
- Автоматический retry при ошибках
- Exponential backoff при rate limiting

### 2. **Авторизованный метод (продвинутый)** 🔐
- Работает даже для приватных инвентарей
- Требует Steam cookies
- Эмулирует настоящий браузер

### 3. **Fallback методы** 🔄
- Альтернативные способы загрузки
- Автоматическое переключение при ошибках

---

## Как это работает

### Публичный метод

```python
from services.steam_authenticated_inventory import SteamAuthenticatedInventory

# Загрузить публичный инвентарь
items = await SteamAuthenticatedInventory.get_inventory_public(
    steam_id="76561198306528518",
    retry_count=3  # Количество попыток
)
```

**Особенности:**
- ✅ Автоматический retry при временных ошибках
- ✅ Exponential backoff (1с, 2с, 4с)
- ✅ Обработка rate limiting (429)
- ✅ Обработка ошибок сервера (500)
- ✅ Правильные заголовки браузера

---

### Авторизованный метод (для приватных инвентарей)

Если инвентарь приватный, можно использовать cookies из браузера:

#### Шаг 1: Получить cookies из браузера

1. Откройте Steam в браузере и авторизуйтесь
2. Откройте DevTools (F12)
3. Перейдите на вкладку Application → Cookies → https://steamcommunity.com
4. Найдите cookies:
   - `sessionid`
   - `steamLoginSecure`

#### Шаг 2: Использовать cookies в API

```python
items = await SteamAuthenticatedInventory.get_inventory_with_cookies(
    steam_id="76561198306528518",
    session_id="ваш_sessionid",
    steam_login_secure="ваш_steamLoginSecure"
)
```

#### Шаг 3: API endpoint

```bash
POST /api/inventory/authenticated/76561198306528518
Content-Type: application/json

{
  "sessionid": "ваш_sessionid",
  "steamLoginSecure": "ваш_steamLoginSecure"
}
```

---

## Почему Steam блокирует запросы?

Steam защищает инвентари от парсеров и ботов:

1. **Rate Limiting** - ограничение количества запросов
2. **User-Agent проверка** - блокировка ботов
3. **Приватность** - закрытые инвентари требуют авторизации
4. **CAPTCHA** - при подозрительной активности

### Наши решения:

✅ **Правильные заголовки** - эмулируем Chrome браузер
✅ **Задержки** - 1-2 секунды между запросами
✅ **Retry логика** - автоматические повторы при ошибках
✅ **Cookies поддержка** - для приватных инвентарей
✅ **Exponential backoff** - увеличение задержки при ошибках

---

## Обработка ошибок

### 403 Forbidden
- **Причина:** Инвентарь приватный
- **Решение:** Использовать авторизованный метод с cookies

### 429 Too Many Requests
- **Причина:** Rate limiting
- **Решение:** Автоматический retry с увеличенной задержкой

### 500 Internal Server Error
- **Причина:** Временная проблема Steam
- **Решение:** Автоматический retry

### Пустой ответ
- **Причина:** Инвентарь пустой или недоступен
- **Решение:** Fallback на тестовые данные для демонстрации

---

## Примеры использования

### Frontend (React)

```typescript
// Загрузить публичный инвентарь
const loadInventory = async (steamId: string) => {
  const response = await fetch(`/api/inventory/${steamId}`);
  const data = await response.json();
  return data.items;
};

// Загрузить с cookies (продвинутый)
const loadInventoryWithCookies = async (steamId: string, cookies: any) => {
  const response = await fetch(`/api/inventory/authenticated/${steamId}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(cookies)
  });
  const data = await response.json();
  return data.items;
};
```

### Backend (Python)

```python
from services.steam_service import SteamService

# Основной метод (автоматически выбирает лучший способ)
items = await SteamService.get_inventory(steam_id)

# Прямой вызов публичного метода
from services.steam_authenticated_inventory import SteamAuthenticatedInventory
items = await SteamAuthenticatedInventory.get_inventory_public(steam_id)

# С cookies
items = await SteamAuthenticatedInventory.get_inventory_with_cookies(
    steam_id=steam_id,
    session_id="xxx",
    steam_login_secure="yyy"
)
```

---

## Тестирование

### Тест 1: Публичный инвентарь
```bash
curl http://localhost:8000/api/inventory/76561198306528518
```

### Тест 2: С cookies
```bash
curl -X POST http://localhost:8000/api/inventory/authenticated/76561198306528518 \
  -H "Content-Type: application/json" \
  -d '{
    "sessionid": "ваш_sessionid",
    "steamLoginSecure": "ваш_steamLoginSecure"
  }'
```

---

## Лучшие практики

1. **Используйте публичный метод по умолчанию** - он работает в 90% случаев
2. **Добавьте задержки** - минимум 1 секунда между запросами
3. **Обрабатывайте ошибки** - всегда имейте fallback
4. **Кешируйте результаты** - не загружайте инвентарь слишком часто
5. **Используйте cookies только при необходимости** - для приватных инвентарей

---

## Сравнение с другими сервисами

### Lis-Skins
- Использует cookies + правильные заголовки
- Наш метод: `SteamAuthenticatedInventory.get_inventory_with_cookies()`

### CSFloat
- Использует публичный API + retry логика
- Наш метод: `SteamAuthenticatedInventory.get_inventory_public()`

### Skinport
- Комбинация методов с fallback
- Наш метод: `SteamService.get_inventory()` (автоматический выбор)

---

## FAQ

**Q: Почему не работает для моего инвентаря?**
A: Проверьте настройки приватности в Steam. Инвентарь должен быть публичным.

**Q: Как часто можно загружать инвентарь?**
A: Рекомендуется не чаще 1 раза в минуту для одного Steam ID.

**Q: Безопасно ли использовать cookies?**
A: Cookies хранятся только в памяти и не сохраняются. Но будьте осторожны - не передавайте их третьим лицам.

**Q: Что делать если Steam возвращает CAPTCHA?**
A: Подождите несколько минут и попробуйте снова. Используйте большие задержки между запросами.

---

## Поддержка

Если у вас проблемы с загрузкой инвентаря:

1. Проверьте логи сервера
2. Убедитесь что инвентарь публичный
3. Попробуйте увеличить задержки
4. Используйте авторизованный метод с cookies

**Логи:** Все методы выводят подробные логи с префиксами:
- `[PUBLIC_INVENTORY]` - публичный метод
- `[AUTH_INVENTORY]` - авторизованный метод
- `[STEAM]` - основной сервис
